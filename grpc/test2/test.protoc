syntax = "proto3";

package service;

option go_package = "./server/service";

message User{
    string name = 1;
    string password = 2;
}

message Login{
    string res = 1;
}

service LoginService {
    rpc CheckLogin(User) returns(Login);
}